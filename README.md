# BY BOOKSHELF DASHBOARD

- clone the project
- then run command "npm install" to install the dependencies
- then run command "npm start" to run the app in local Host

# Live Link 
- https://my-book-shelf-sigma.vercel.app/
